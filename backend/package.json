{
  "name": "tiktok-live-monitor-backend",
  "version": "1.0.0",
  "scripts": {
    "docker:build": "docker compose build",
    "docker:up": "docker compose up",
    "postinstall": "patch-package",
    "test:bridge": "node --test internal/monitor/*.test.js"
  },
  "author": "",
  "license": "ISC",
  "description": "TikTok Live Monitor (lite, sem IA)",
  "dependencies": {
    "patch-package": "8.0.1",
    "tiktok-live-connector": "2.4.4"
  },
  "devDependencies": {
    "railway": "^3.11.0"
  }
}
